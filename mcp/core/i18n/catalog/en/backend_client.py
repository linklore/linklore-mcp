"""English (en) messages for the 'backend_client' surface."""
MESSAGES: dict[str, str] = {
    "err_keychain_timeout": "Keychain lookup (security find-generic-password) timed out ({timeout_s}s)",
    "err_generic_status": "server error (HTTP {status})",
}
