"""English (en) messages for the 'link_classify' surface."""
MESSAGES: dict[str, str] = {

    "warn_unresolved": "⚠️ links= resolution failed: '{token}' (no id/title/file match)",
    "hint_similar_file": "   similar file: {files}",
    "hint_retry_exact_path": "   → if that's it, retry with the exact path",
}
