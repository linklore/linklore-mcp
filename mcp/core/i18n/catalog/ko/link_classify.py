"""Korean (ko) messages for the 'link_classify' surface."""
MESSAGES: dict[str, str] = {

    "warn_unresolved": "⚠️ links= 해석 실패: '{token}' (id·제목·파일 매칭 안 됨)",
    "hint_similar_file": "   비슷한 파일: {files}",
    "hint_retry_exact_path": "   → 맞으면 정확 경로로 재시도",
}
